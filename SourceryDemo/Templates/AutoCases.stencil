// AutoCases
{% for enum in types.enums %}
extension {{ enum.name }} {
  static let count = {{ enum.cases.count }}
  
  static let allCases: [{{ enum.name }}] = [
  {% for case in enum.cases %}  .{{ case.name }}{% if not forloop.last %},{% endif %}
  {% endfor %}]
}
{% endfor %}